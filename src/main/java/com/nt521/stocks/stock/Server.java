package com.nt521.stocks.stock;

public class Server implements Process {
    @Override
    public void updatePrice() {
        // Update data in database
    }

    @Override
    public void pushNotification() {
        // Send notification to user via broadcast channel
    }
}
